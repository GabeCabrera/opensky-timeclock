{
  "name": "opensky-timeclock",
  "version": "1.0.0",
  "description": "A simple time clock app with React frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["timeclock", "react", "nodejs", "postgresql"],
  "author": "OpenSky",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}